---
name: story-handoff-commit
description: Generate a handoff document from commit-linked Story evidence.
---

Run `story handoff commit --commit <commitish> --json`. Summarize the objective,
current state, completed work, unresolved questions, and next steps from the
returned artifact. Use `story checkpoint show` when more transcript context is
needed.
